# hm-MES (第一批/S1) 前端功能验收清单

本文档列出了根据《hm-MES前端详细开发任务清单》已完成的第一批前端开发内容。您可以按照以下步骤与检查项进行交互测试。

> **提示：** 
> 验收前端功能时，如果后端接口及数据库字段尚未准备完毕，在进行“保存/修改”操作时可能会抛出接口 404 或参数不存在的报错。
> 此时，建议按 `F12` 打开浏览器开发者工具，在 **Network（网络）** 面板中查看接口请求的 **Payload / Request Body**。只要新增的字段准确提交，即代表前端已正确拦截和绑定该业务！

---

## 1. 项目管理模块 (M1)

此模块新建了项目的基础维护功能，为后续业务单据（如销售订单、生产计划）的项目化管理提供数据支撑。

- **源码路径**：
  - 视图页：`src/views/erp/project/project/index.vue`
  - 表单页：`src/views/erp/project/project/ProjectForm.vue`
  - API：`src/api/erp/project/index.ts`
- **验收点**：
  - [ ] **菜单路由**：手动将该组件路径配置到后台系统菜单后，能够正常打开“项目管理”页面。
  - [ ] **新建项目**：点击【新增】按钮，正常弹出表单。输入“项目名称、负责人、开始日期、结束日期、描述”并提交。
  - [ ] **列表展示**：表格能正确展示包含“项目状态（进行中/已完成/延期等）”在内的数据。
  - [ ] **搜索过滤**：顶部搜索栏能通过“名称、项目编号、负责人”等条件筛选列表。
  - [ ] **修改与删除**：点击表格右侧【编辑】能否正确回显数据并修改；【删除】按钮有二次确认并且能发起删除请求。

---

## 2. 销售订单模块（项目化与交期改造）

在原有 ERP 销售订单体系上扩展，要求支持按项目接单，并明确要求交期。

- **源码路径**：
  - 视图页：`src/views/erp/sale/order/index.vue`
  - 表单页：`src/views/erp/sale/order/SaleOrderForm.vue`
  - API：`src/api/erp/sale/order/index.ts`
- **验收点**：
  - [ ] **新增表单增强**：点击【新增销售订单】，检查弹出表单中是否增加了 **「所属项目」**（下拉单选）和 **「交货日期」**（日期控件）。
  - [ ] **项目数据联动**：点击 **「所属项目」** 下拉框，是否能正常加载项目管理模块中的精简列表（ID与名称）。
  - [ ] **数据提交**：填写订单数据保存时，F12 的 Payload 请求体重是否包含了 `projectId` 与 `deliveryDate` 字段。
  - [ ] **列表查询增强**：在销售订单列表的顶部搜索栏，是否增加了 **「项目名称」** 的查询项。
  - [ ] **列表展示增强**：销售订单列表的表格列中，是否新增并正确展示了 **「所属项目」** 和 **「交期」** 两列数据。

---

## 3. 产品基础数据扩展（制造与 MRP 属性补强）

为了支撑 MES 生产制造和 MRP 物料需求计划，对系统现有的“产品”对象增加业务属性。

- **源码路径**：
  - 视图页：`src/views/erp/product/product/index.vue`
  - 表单页：`src/views/erp/product/product/ProductForm.vue`
  - API：`src/api/erp/product/product/index.ts`
  - 字典设置：`src/utils/dict.ts`
- **验收点**：
  - [ ] **字典扩展**：全局自动加载了 `ERP_SUPPLY_TYPE` （供给方式）字典选项。
  - [ ] **表单属性录入**：点击【新增/编辑产品】，表单下方将多出四个配置项：
    1. **参与 MRP**：Switch 开关组件，默认开启 (`true`)。
    2. **供给方式**：Select 下拉框（选项应为字典加载出的等值）。
    3. **默认供应商**：Select 下拉框（可输入过滤），数据由现有供应商接口提供。
    4. **默认工艺路线**：Select 下拉框（可输入过滤），数据由新增的 API 提供。
  - [ ] **数据验证**：确保这四项被填写或更改后，保存提交时的 Payload 中包含了这4个属性（`mrpEnable`, `supplyType`, `defaultSupplierId`, `defaultRouteId`）。
  - [ ] **列表直观展示**：在产品列表中，表格已新增 **「参与 MRP」** 及 **「供给方式」** 的展示列，并通过 `dict-tag` 标签美化显示。

---

## 4. MRP 计划参数模块 (M2专属)

独立维护特定产品在生产或采购时的补货逻辑。

- **源码路径**：
  - 视图页：`src/views/erp/mrp/plan-rule/index.vue`
  - 表单页：`src/views/erp/mrp/plan-rule/PlanRuleForm.vue`
  - API：`src/api/erp/mrp/plan-rule/index.ts`
- **验收点**：
  - [ ] **菜单路由**：手动绑定后台路由后，正常进入计划参数的列表页面。
  - [ ] **新建计划参数**：打开表单，检查以下必填与非必填输入项的验证规则与控件是否可用：
    - 产品（下拉请求产品精简列表，带搜索，必填）
    - 供给方式（字典下拉，必填）
    - 安全库存（数字录入框，最低为0）
    - 最小采购量（数字录入框，最低为0）
    - 倍量（数字录入框，最低为 1）
    - 采购提前期(天) 及 生产提前期(天) （数字录入框）
    - 默认供应商（下拉搜索现存供应商）
    - 状态（单选开启/关闭开关）
  - [ ] **数据提交**：测试新增或编辑表单提交时，前端校验能否拦截为空的必填项，验证通过后正常发出网络请求。
  - [ ] **高级搜索**：顶部搜索栏能够支持使用“选择产品”或“启用状态”定位筛选特定计划参数数据。
  - [ ] **列表展示**：能够正确解析并显示产品名称、供应商名称等业务名称级数据，直观体现出对应的值而非数字 ID。

# 下周开发计划 - 2026年6月29日至7月3日

## 聚焦方向：前后端开发任务

基于代码审查发现的问题，下周将聚焦于具体的前后端开发任务，修复关键问题并优化系统功能。

---

## 🔧 后端开发任务

### 任务1：修复资产折旧事务安全问题（优先级：最高）
**预计工时**：3小时  
**负责模块**：财务模块 - 资产折旧

**问题描述**：
- `ErpFinanceAssetDepreciationServiceImpl.java` 在事务内调用外部API `deptApi.getDept()`
- 违反AGENTS.md铁律1：事务内禁止调外部系统
- 风险：外部API超时会导致整个事务回滚，数据丢失

**开发内容**：
1. 重构 `generateDepreciation()` 方法，将外部API调用移到事务外
2. 实现批量预加载部门信息，减少RPC调用次数
3. 添加异常处理和日志记录

**验收标准**：
- [ ] 事务内不再调用外部API
- [ ] 折旧生成功能正常工作
- [ ] 凭证biz_type正确（70/71/72）
- [ ] 单元测试覆盖关键路径

---

### 任务2：修复三单匹配安全漏洞（优先级：最高）
**预计工时**：2小时  
**负责模块**：财务模块 - 三单匹配

**问题描述**：
- `ErpThreeWayMatchController.java` 发票金额由前端传入，存在安全风险
- 前端可篡改金额，导致三单匹配逻辑失效

**开发内容**：
1. 修改接口，通过发票号从系统查询金额
2. 添加后端金额校验逻辑
3. 定义业务异常错误码

**验收标准**：
- [ ] 发票金额不再由前端传入
- [ ] 三单匹配逻辑正确
- [ ] 异常提示友好

---

### 任务3：修复N+1查询性能问题（优先级：高）
**预计工时**：4小时  
**负责模块**：财务模块 - 资产折旧、生产成本

**问题描述**：
- 折旧循环内逐条查询折旧记录是否存在
- 生产成本环比计算递归调用导致重复计算
- 趋势页面逐月调用API

**开发内容**：
1. 重构折旧查询，改为批量查询
2. 优化环比计算逻辑，避免递归调用
3. 实现批量查询接口，支持多月份数据一次性返回

**验收标准**：
- [ ] SQL执行次数减少80%以上
- [ ] 页面响应时间明显提升
- [ ] 功能逻辑正确

---

### 任务4：定义业务异常错误码（优先级：高）
**预计工时**：2小时  
**负责模块**：财务模块、文件管理模块

**问题描述**：
- 新增模块全部使用 `RuntimeException`，违反项目规范
- 应使用框架的 `ErrorCode` 机制

**开发内容**：
1. 定义 `ErpFinanceErrorCodeConstants` 错误码
2. 定义 `ErpFileErrorCodeConstants` 错误码
3. 替换所有 `new RuntimeException(...)` 为 `exception(XXX_NOT_EXISTS)`

**验收标准**：
- [ ] 所有业务异常使用统一错误码
- [ ] 异常信息友好，便于排查
- [ ] 无 `RuntimeException` 直接抛出

---

### 任务5：修复SQL幂等性问题（优先级：高）
**预计工时**：30分钟  
**负责模块**：基础设施 - SQL脚本

**问题描述**：
- `148-erp-file-management-enhancement.sql` 的 ALTER TABLE 语句不具备幂等性
- 重复执行会导致脚本失败

**开发内容**：
1. 修改SQL脚本，添加条件判断
2. 确保脚本可重复执行

**验收标准**：
- [ ] SQL脚本可重复执行
- [ ] 不影响现有数据

---

## 🎨 前端开发任务

### 任务6：修复颜色规范违反问题（优先级：高）
**预计工时**：1小时  
**负责模块**：财务模块 - 成本分析

**问题描述**：
- `cost-product/index.vue` 和 `trend.vue` 使用紫色系颜色
- 违反AGENTS.md 3.1.7 颜色约束

**开发内容**：
1. 替换 `#8B5CF6`（紫色）为 `#06B6D4`（青色）
2. 替换 `#EC4899`（粉紫色）为 `#14B8A6`（蓝绿色）
3. 验证图表颜色区分度

**验收标准**：
- [ ] 无紫色系颜色出现
- [ ] 图表颜色区分度足够
- [ ] 视觉效果符合企业级标准

---

### 任务7：优化前端API调用（优先级：高）
**预计工时**：2小时  
**负责模块**：财务模块 - 资产管理

**问题描述**：
- `checkPeriodStatus` 每次选择期间都重新查询账簿
- 前端组件动态import加载axios

**开发内容**：
1. 缓存账簿信息，避免重复查询
2. 将动态import改为静态import
3. 优化组件加载性能

**验收标准**：
- [ ] 网络请求数量减少
- [ ] 页面加载速度提升
- [ ] 功能正常

---

### 任务8：修复文件删除逻辑（优先级：高）
**预计工时**：30分钟  
**负责模块**：基础设施 - 文件管理

**问题描述**：
- 删除附件只删业务关联，不删实际文件
- 会导致 `infra_file` 表中产生孤立记录

**开发内容**：
1. 修改删除逻辑，同时调用文件删除接口
2. 添加异常处理

**验收标准**：
- [ ] 删除附件时同时删除文件
- [ ] 无孤立记录产生

---

### 任务9：完善文件夹树形组件（优先级：中）
**预计工时**：3小时  
**负责模块**：基础设施 - 文件管理

**问题描述**：
- `FileFolderTree.vue` 接口支持树形结构，但UI是扁平列表
- 缺少展开/折叠功能

**开发内容**：
1. 使用 Element Plus 的 `el-tree` 组件重构
2. 实现文件夹层级展开/折叠
3. 优化交互体验

**验收标准**：
- [ ] 文件夹树形结构正确显示
- [ ] 展开/折叠功能正常
- [ ] 交互体验流畅

---

### 任务10：修复Controller返回DO问题（优先级：中）
**预计工时**：1.5小时  
**负责模块**：基础设施 - 文件管理

**问题描述**：
- `FileFolderController.java` 和 `FileBizRelController.java` 直接返回DO对象
- 暴露了数据库字段结构

**开发内容**：
1. 定义 `FileFolderRespVO` 和 `FileBizRelRespVO`
2. 使用 BeanUtils 转换
3. 修改Controller返回类型

**验收标准**：
- [ ] 接口返回VO对象
- [ ] 不暴露数据库结构
- [ ] 功能正常

---

## 📊 任务汇总

| 任务 | 类型 | 优先级 | 预计工时 | 负责模块 |
|------|------|--------|----------|----------|
| 任务1：资产折旧事务安全 | 后端 | 最高 | 3小时 | 财务模块 |
| 任务2：三单匹配安全漏洞 | 后端 | 最高 | 2小时 | 财务模块 |
| 任务3：N+1查询性能优化 | 后端 | 高 | 4小时 | 财务模块 |
| 任务4：业务异常错误码 | 后端 | 高 | 2小时 | 财务/文件模块 |
| 任务5：SQL幂等性 | 后端 | 高 | 30分钟 | 基础设施 |
| 任务6：颜色规范修复 | 前端 | 高 | 1小时 | 财务模块 |
| 任务7：前端API优化 | 前端 | 高 | 2小时 | 财务模块 |
| 任务8：文件删除逻辑 | 前端 | 高 | 30分钟 | 基础设施 |
| 任务9：文件夹树形组件 | 前端 | 中 | 3小时 | 基础设施 |
| 任务10：Controller返回VO | 后端 | 中 | 1.5小时 | 基础设施 |

**总计预计工时**：19.5小时

---

## 📅 每日安排建议

### 周一（6月29日）
- 上午：任务1（资产折旧事务安全）
- 下午：任务2（三单匹配安全漏洞）

### 周二（6月30日）
- 上午：任务3（N+1查询性能优化）- 折旧查询优化
- 下午：任务3（N+1查询性能优化）- 环比计算优化

### 周三（7月1日）
- 上午：任务4（业务异常错误码）
- 下午：任务5（SQL幂等性）+ 任务6（颜色规范修复）

### 周四（7月2日）
- 上午：任务7（前端API优化）
- 下午：任务8（文件删除逻辑）+ 任务10（Controller返回VO）

### 周五（7月3日）
- 上午：任务9（文件夹树形组件）
- 下午：代码审查、测试验证、文档更新

---

## ⚠️ 风险与依赖

### 风险
1. **安全风险**：任务1和任务2涉及财务数据安全，必须优先完成
2. **性能风险**：任务3的N+1问题在数据量增长时会影响系统性能
3. **兼容性风险**：任务4的错误码重构可能影响现有异常处理逻辑

### 依赖
1. **测试环境**：需要测试环境验证修复效果
2. **业务确认**：任务2的发票金额查询方案需要业务方确认
3. **代码审查**：每个任务完成后需要进行代码审查

---

## 🎯 验收标准

### 后端验收
- [ ] 所有CRITICAL问题修复完成
- [ ] 单元测试覆盖率不低于现有水平
- [ ] 无新增RuntimeException直接抛出
- [ ] SQL执行次数优化明显

### 前端验收
- [ ] 无紫色系颜色出现
- [ ] 网络请求数量减少
- [ ] 文件管理功能完整
- [ ] 交互体验流畅

### 整体验收
- [ ] 所有任务完成
- [ ] 代码审查通过
- [ ] 测试环境验证通过
- [ ] 文档更新完成
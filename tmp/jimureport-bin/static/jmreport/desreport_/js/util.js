'use strict';const util={setting2Option(setting){let option={};Object.keys(setting).map(key=>{let str=setting[key];if(str&&typeof str=="object"&&str.constructor==Array)option[key]=[...str];else if(key.indexOf("_")>0){let arr=key.split("_");this.handleMultiUnderline(str,option,arr,0)}else option[key]=str});return option},handleMultiUnderline(value,obj,arr,index){let key=arr[index];if(index==arr.length-1){obj[key]=value;return}if(!obj[key])obj[key]={};this.handleMultiUnderline(value,obj[key],arr,++index)},
percentFormat(val){return val+"%"},setting2Array(setting){let array=[];setting.forEach(item=>{let option=this.setting2Option(item);array.push(option)});return array},getNumberFromPercent(p,suffix=""){if(!p)return 0;return Number((p+"").replace("%","").replace(suffix,""))},arrayGroupBy(list,field){let sorted=this.groupBy(list,function(item){return[item[field]]});return sorted},groupBy(array,f){let groups={};array.forEach(function(o){let group=JSON.stringify(f(o));groups[group]=groups[group]||[];groups[group].push(o)});
return Object.keys(groups).map(function(group){return groups[group]})},sortFun(arr){return function(a,b){let v1=a[arr];let v2=b[arr];return v1-v2}}};